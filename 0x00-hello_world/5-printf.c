#include <stdio.h>

/**
 * main - prints the phrase "with proper grammar,
 * but the outcome is a piece of art"
 * followed by a new line
 * Must use printf not puts
 * Return: the value 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
