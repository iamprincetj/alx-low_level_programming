#include <stdio.h>

/**
 *main - prints its name, followed by a new name
 *@argc: the argument count
 *@argv: the argement vector
 *
 *Return: Always 0
 */

int main(int argc, char **argv)
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
